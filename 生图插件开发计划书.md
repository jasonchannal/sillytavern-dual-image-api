# SillyTavern 双模式生图插件开发计划书

## 1. 最终目标

做一个 SillyTavern 第三方生图插件，让用户可以在聊天里独立调用第三方生图 API，并根据提示词内容自动判断使用 SFW 或 NSFW 模式。

两个模式需要完全独立配置：

- SFW 模式：独立 API 地址、模型、密钥、默认参数、提示词前后缀、负面提示词。
- NSFW 模式：独立 API 地址、模型、密钥、默认参数、提示词前后缀、负面提示词。

用户正常只需要输入生图提示词，插件自动判断模式、调用对应 API、生成图片，并把图片保存/插入到当前聊天中。

## 2. 推荐方案

推荐做成“前端扩展 + 服务端插件”的组合，而不是纯前端扩展。

原因：

- 第三方生图 API 往往不允许浏览器直接请求，容易遇到跨域限制。
- API Key 不应该长期暴露在浏览器扩展代码或前端设置里。
- 不同生图 API 返回格式差异很大，需要一个服务端代理统一转成 SillyTavern 可用的图片结果。
- SFW/NSFW 两套配置如果都带密钥，服务端保存和调用更稳妥。

整体形态：

- 前端扩展：负责设置界面、按钮、斜杠命令、提示词输入、模式提示、聊天插图。
- 服务端插件：负责保存密钥、调用第三方 API、处理图片 URL/base64、统一错误信息。
- 共享规则层：负责 SFW/NSFW 判断、参数校验、接口配置结构。

## 3. MVP 范围

第一版只做稳定可用的核心闭环。

必须包含：

- 一个生图入口按钮。
- 一个斜杠命令，例如 `/dualimg prompt...`。
- SFW / NSFW 两个配置页签。
- 每个模式可分别设置 API 类型、API 地址、模型、密钥、图片尺寸、步数、CFG、负面提示词。
- 自动模式判断：根据提示词内容选择 SFW 或 NSFW。
- 手动覆盖：用户可以强制指定 `mode=sfw`、`mode=nsfw` 或 `mode=auto`。
- 生成中状态、失败提示、取消生成。
- 生成成功后保存图片，并插入当前聊天。
- 测试连接按钮，分别测试 SFW 和 NSFW 配置。

第一版不做：

- 不做模型训练。
- 不做复杂工作流编辑器。
- 不替换 SillyTavern 内置 Image Generation。
- 不默认拦截所有聊天消息自动生图。
- 不绕过第三方 API 的内容规则。
- 不把密钥写入前端本地设置或聊天记录。

## 4. 插件结构

建议仓库结构：

```text
st-dual-image-api/
  extension/
    manifest.json
    index.js
    settings.html
    style.css
    i18n/
      zh-cn.json
      en.json
  server-plugin/
    package.json
    index.js
    adapters/
      openai-compatible.js
      generic-json.js
      image-url.js
    storage/
      profile-store.js
      secret-store.js
  shared/
    defaults.js
    classifier.js
    schema.js
  README.md
```

实际安装时：

- `extension/` 放到 SillyTavern 的第三方扩展目录。
- `server-plugin/` 放到 SillyTavern 的服务端插件目录。
- `shared/` 可在打包时复制到两端，或拆成两份轻量逻辑。

## 5. 用户体验设计

设置界面分为四块：

1. 总开关

- 启用插件。
- 默认模式：自动 / 强制 SFW / 强制 NSFW。
- 是否允许 NSFW 模式。
- 模式判断不确定时的处理：默认走 SFW。

2. SFW 配置

- API 类型。
- Base URL。
- Endpoint。
- API Key。
- 模型名。
- 默认尺寸。
- 默认负面提示词。
- 测试连接。

3. NSFW 配置

- 与 SFW 相同，但完全独立保存。
- 可以使用另一个 API、另一个模型、另一套密钥。

4. 高级设置

- 自动判断关键词表。
- 模式判断置信度阈值。
- 请求超时。
- 重试次数。
- 是否在生成结果旁显示本次使用的模式。
- 是否保存原始提示词到图片元数据。

聊天里的体验：

- 用户点击按钮，弹出输入框。
- 用户输入提示词并确认。
- 插件显示“正在生成图片”，同时标记当前使用 SFW 或 NSFW。
- 生成成功后图片直接出现在聊天中。
- 生成失败时显示普通用户能理解的原因，例如“NSFW API 未配置”“第三方接口超时”“没有返回图片”。

## 6. SFW/NSFW 自动切换规则

第一版采用本地规则判断，不把用户提示词额外发给第三方分类服务。

判断流程：

1. 规范化提示词

- 转小写。
- 去掉多余符号。
- 识别中英文关键词。
- 识别常见 tag 写法，例如逗号分隔、括号权重、booru 风格标签。

2. 安全拦截

遇到明显不应生成的内容，不进入 NSFW API，而是直接拒绝或提示用户修改。

必须拦截：

- 未成年人或年龄不明角色的性内容。
- 非自愿、胁迫、伤害相关性内容。
- 明显违法或高风险内容。

3. NSFW 评分

命中成人、裸露、性行为、性器官、强性暗示等关键词时提高 NSFW 分数。

4. SFW 修正

命中“safe”“sfw”“portrait”“fully clothed”“普通立绘”“头像”“风景”等语义时降低 NSFW 分数。

5. 最终决策

- 分数高于阈值：走 NSFW。
- 分数低于阈值：走 SFW。
- 不确定：默认走 SFW，并允许用户手动改为 NSFW。

6. 手动优先

用户显式写 `mode=sfw` 或 `mode=nsfw` 时优先使用用户指定模式，但仍保留安全拦截。

这个判断器不能保证 100% 准确，所以第一版要提供清楚的模式显示和手动覆盖。

## 7. API 适配方案

第一版优先支持两类 API：

1. OpenAI-compatible 生图接口

适合很多第三方聚合 API。

默认请求形式：

```json
{
  "model": "{{model}}",
  "prompt": "{{prompt}}",
  "n": 1,
  "size": "{{width}}x{{height}}",
  "response_format": "b64_json"
}
```

支持返回：

- `data[0].b64_json`
- `data[0].url`
- `image`
- `images[0].url`

2. Generic JSON 接口

用户可以配置：

- 请求方法。
- 请求 URL。
- Headers。
- 请求体模板。
- 图片结果字段路径。
- 图片是 URL 还是 base64。

这样可以接入更多非标准第三方 API。

第二版再支持：

- 异步任务型 API。
- Webhook 或轮询。
- 多图返回。
- 图生图。
- ControlNet / 参考图。
- ComfyUI 工作流。

## 8. 请求流程

标准生成流程：

1. 用户输入提示词。
2. 前端扩展读取当前插件设置。
3. 分类器判断 SFW / NSFW。
4. 前端把提示词、模式和参数发给服务端插件。
5. 服务端插件读取对应模式的 API 配置和密钥。
6. 服务端插件调用第三方生图 API。
7. 如果第三方返回图片 URL，服务端下载图片。
8. 服务端统一返回 base64 图片和格式。
9. 前端调用 SillyTavern 的图片保存能力。
10. 前端把图片作为媒体插入当前聊天。

失败时：

- 不把 API Key 打到日志。
- 不把第三方原始长错误直接暴露给用户。
- 前端只展示简短、可行动的错误说明。
- 详细错误只保留在开发日志里，并自动脱敏。

## 9. 数据与设置

普通设置可以保存在前端扩展设置中：

- 是否启用。
- 默认模式。
- 尺寸、步数、CFG。
- 提示词前后缀。
- 负面提示词。
- 自动判断关键词表。

敏感信息必须保存在服务端：

- SFW API Key。
- NSFW API Key。
- 自定义 Header 中的授权字段。

服务端读取设置时按用户隔离：

- 单用户部署：保存到插件自己的数据文件。
- 多用户部署：按 SillyTavern 用户目录或用户 ID 分开保存。

前端读取密钥状态时只返回：

```json
{
  "sfwKeyConfigured": true,
  "nsfwKeyConfigured": false
}
```

不返回真实密钥。

## 10. 安全与边界

必须遵守：

- 不在前端显示完整 API Key。
- 不在日志打印 API Key。
- 不把用户提示词发给额外分类服务，除非用户主动开启。
- 不自动公开、上传或分享生成图片。
- 不默认启用 NSFW，用户需要明确打开。
- 对未成年人、非自愿、伤害等高风险内容直接拦截。
- 第三方 API 拒绝时不尝试绕过。

## 11. 开发阶段

第一阶段：调研与骨架

- 建立扩展目录和服务端插件目录。
- 做出设置 UI 草稿。
- 接入一个最小可用的服务端测试接口。
- 验证 SillyTavern 能加载扩展和服务端插件。

验收标准：

- 扩展能在 SillyTavern 扩展列表出现。
- 设置界面能打开。
- 服务端插件能返回健康检查结果。

第二阶段：配置与密钥

- 完成 SFW / NSFW 两套配置。
- 完成密钥保存、读取状态、删除。
- 完成测试连接按钮。

验收标准：

- 两套 API 配置互不影响。
- 前端看不到完整密钥。
- 错误配置能给出清楚提示。

第三阶段：生图闭环

- 支持 OpenAI-compatible 接口。
- 支持 base64 和 URL 图片返回。
- 生成图片后保存并插入聊天。
- 加入生成中、取消、失败状态。

验收标准：

- SFW 配置可生成图片。
- NSFW 配置可生成图片。
- 图片能进入当前聊天。
- 生成失败不会卡死界面。

第四阶段：自动模式判断

- 实现本地关键词评分。
- 加入安全拦截。
- 加入手动模式覆盖。
- 在 UI 中显示本次使用的模式。

验收标准：

- 普通提示词默认走 SFW。
- 成人向提示词走 NSFW。
- 不确定提示词默认走 SFW。
- `mode=sfw` / `mode=nsfw` 可覆盖自动判断。
- 高风险内容不进入第三方生图 API。

第五阶段：通用 API 适配

- 增加 Generic JSON 配置。
- 支持请求体模板。
- 支持结果字段路径。
- 支持自定义 Header。

验收标准：

- 不改代码即可接入至少一个非 OpenAI-compatible API。
- 配置错误时能定位到 URL、鉴权、字段路径或返回格式问题。

第六阶段：打磨与交付

- 写 README。
- 写安装说明。
- 写常见问题。
- 做基础兼容测试。
- 整理默认关键词表。

验收标准：

- 新用户按文档能完成安装。
- 不看代码也能配置两套 API。
- 常见失败场景有明确提示。

## 12. 测试计划

功能测试：

- 插件加载。
- SFW 配置保存。
- NSFW 配置保存。
- 密钥保存后前端只显示已配置状态。
- 测试连接成功和失败。
- SFW 生成成功。
- NSFW 生成成功。
- 手动取消生成。
- API 超时。
- API 返回空图片。
- API 返回 URL。
- API 返回 base64。

分类测试：

- 普通头像、风景、服装、角色立绘走 SFW。
- 明确成人内容走 NSFW。
- 模糊内容走 SFW。
- 手动覆盖生效。
- 高风险内容被拦截。

界面测试：

- 桌面端设置界面不挤压。
- 小窗口下输入框和按钮不重叠。
- 生成中状态清楚。
- 错误提示不暴露技术细节。

安全测试：

- 日志不出现密钥。
- 前端设置不出现密钥明文。
- 失败响应不回传完整鉴权 Header。
- 多用户部署时配置不串号。

## 13. 主要风险

1. SillyTavern 服务端插件依赖

如果用户的 SillyTavern 没有启用服务端插件，纯前端版本会遇到密钥安全和跨域问题。

处理方案：

- MVP 明确要求服务端插件。
- 纯前端直连只作为开发调试选项，不作为推荐方案。

2. 第三方 API 差异大

不同 API 的字段、鉴权、返回图片方式差异很大。

处理方案：

- 第一版先支持 OpenAI-compatible。
- 第二步支持 Generic JSON。
- 异步任务型 API 放到后续版本。

3. 自动分类不可能完全准确

提示词可能含糊、反讽、缩写或混合语言。

处理方案：

- 不确定默认 SFW。
- 显示本次使用模式。
- 支持手动覆盖。
- 支持用户编辑关键词表。

4. NSFW 合规边界

不同 API、地区和使用场景对 NSFW 内容限制不同。

处理方案：

- 插件只做模式路由，不绕过第三方规则。
- 高风险内容本地拦截。
- NSFW 需要用户主动开启。

## 14. 后续增强

可在 MVP 稳定后继续做：

- 图片历史记录面板。
- 一键重试。
- 使用同一提示词切换 SFW/NSFW 重生。
- 多图生成。
- 图生图。
- 参考图上传。
- 每个角色绑定默认生图风格。
- 每个聊天绑定默认模式策略。
- 使用本地小模型或当前聊天模型辅助判断模式。
- 支持异步任务型 API。
- 支持 ComfyUI 工作流。

## 15. 验收标准总表

插件可以交付时应满足：

- 用户能在 SillyTavern 中看到并打开插件设置。
- 用户能分别配置 SFW 和 NSFW 两套 API。
- API Key 不暴露在前端设置和日志中。
- 用户能通过按钮或斜杠命令生成图片。
- 插件能根据提示词自动选择 SFW 或 NSFW。
- 用户能手动覆盖自动判断。
- 生成成功后图片能保存并插入当前聊天。
- 常见失败场景有清楚提示。
- 不确定或高风险内容不会误入 NSFW API。
- 不需要修改 SillyTavern 核心代码即可安装使用。

## 16. 参考依据

- SillyTavern 官方扩展开发说明：https://docs.sillytavern.app/for-contributors/writing-extensions/
- SillyTavern 官方服务端插件说明：https://docs.sillytavern.app/for-contributors/server-plugins/
- SillyTavern 内置 Image Generation 扩展与服务端接口实现：https://github.com/SillyTavern/SillyTavern
